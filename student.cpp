#include "student.hpp"
#include "iostream"
#include <string>
student::student()

    :name
     ,note(MAX_NOTE)
     ,surname
     ,coeff(COEFF) 
     

{}
    student::~student(){}



void student::addNote(name, note, surname, number)
{
    int totalNote = 0;
    if(name != '\n'){
        if(surname = '\n'){
            if(number != 0){
                this->tolatNote += note;
            }
        }
    }
}

void student::calculate_average(totalNote, coeff)
{
    int average = 0;
    this->average = totalNote / coeff;
   
}
int student::admitted(note)
{
    if(average > 11.5)
    return this->admitted;
}
int student:: Maximum(average[N])
{
    int max =0;
    for (int i=1 ; i<N ;i++)
    {
        average[i] < max;
    }
    return this->max;
}
int student::Minimum(average[N])
{
    int min = 0;
    for(i=1 ; i <N ; i++)
    {
        average[i] > min;
    }
    return this->min;
}

int student::calculate_Mediane()
{
    return this->(min + max)/2;
}
void student::tri_average(average[N])
{
    for (i=1 ; i<N; i++)
    {
        if(average[i] > averge[i+1]
        {
            average[i] = average[i+1]    
        }
    }
}